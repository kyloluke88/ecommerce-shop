import { ComingSoonPage } from "@/features/store/components/coming-soon-page";

export default function AccountPage() {
  return (
    <ComingSoonPage
      title="Account"
      description="User account pages (profile, orders, address book) will be migrated here in TypeScript components."
    />
  );
}
